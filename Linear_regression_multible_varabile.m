[X,y]=loadData();
[X,mue,sigma]=featureNormalization(X);
theta=zeros(size(X,2)+1,1);
lambda=0;
alpha = 0.01;
num_iterations = 3000;
%[J,grad]=costFunction(X,y,theta);
%options=optimset('MaxIter',1500,'GradObj','on');
%[theta,cost] =fminunc(@(t) costFunction(X,y,theta,lambda),theta,options);
[theta,j_history] = gradient(X,ynorm,theta,alpha,num_iterations);
